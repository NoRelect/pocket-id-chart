apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pocket-id.fullname" . }}-test-connection"
  labels:
    {{- include "pocket-id.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pocket-id.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
